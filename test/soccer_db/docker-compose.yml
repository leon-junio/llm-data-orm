version: '3.8'

services:
  mysql:
    image: mysql:latest
    container_name: mysql_tcc
    restart: always
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: "soccer_test"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./test_db.sql:/docker-entrypoint-initdb.d/soccer_db.sql

volumes:
  mysql_data:
    driver: local